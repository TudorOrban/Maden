version: api/v1
kind: Deployment
spec:
  name: example-deployment-6
  replicas: 1
  selector:
    matchLabels:
      app: example
  template:
    metadata:
      labels:
        app: example
    spec:
      containers:
      - name: example
        image: example:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
spec:
  name: example-service
  selector:
    app: example
  ports:
    - protocol: TCP
      port: 80
      targetPort: 81
  type: LoadBalancer